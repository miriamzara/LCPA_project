% Page setup
\usepackage[inner=20mm,
            outer=49mm,% = marginparsep + marginparwidth + 5mm (between marginpar and page border)
            top = 20mm,
            bottom = 25mm,
            marginparsep = 6mm,
            marginparwidth = 30mm,
            left = 25mm,
            right = 25mm]{geometry}
\usepackage{fancyhdr}
    \pagestyle{fancy}
    \fancyhf{}
    \fancyhead[RE]{\ifnum\value{chapter}>0\nouppercase{\leftmark}\fi}
    \fancyhead[LE]{\small\textbf{\thepage}}
    \fancyhead[LO]{\nouppercase{\rightmark}}
    \fancyhead[RO]{\small\textbf{\thepage}}
    \setlength{\headheight}{13.6pt}         % Adjust the headheight
    \addtolength{\topmargin}{-1.6pt}        % Optionally adjust the topmargin

    \renewcommand{\chaptermark}[1]{         % Chapter style in header
        \markboth{
            \MakeUppercase{\chaptername}
            \thechapter : #1
            }{}
        }


% Basic text and fonts packages
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{comment}

\setlength{\parskip}{0pt}       % No skip between paragraphs
\setlength{\parindent}{0pt}     % No indentation at new paragraphs

% Multiple files
\usepackage{subfiles}

% For scientific writing and equations
\usepackage{bm, bbold}                  % for addiotional bold characters
%\usepackage{dutchcal}                   % for lowercase calligraphic letters
\usepackage{mathtools, amssymb, amsthm} % for additional environments and symbols
\usepackage{cancel}		% to cancel terms in equations
\usepackage[skins, breakable, theorems]{tcolorbox}  % % these three are for the definition/etc. boxes
\usepackage{framed}                                 %
\usepackage{mdframed}                               %
\usepackage{physics} % for easier derivatives, integrals, vector notation etc.

% Tables
\usepackage{booktabs}
\usepackage{multirow}

% Figures and graphs
\usepackage{graphicx}       % to insert figures
\graphicspath{{Figures/}}   % default file path for figures
\usepackage{color}
\usepackage[font = small,   % caption style etc
            labelfont = bf,
            skip = 10pt,
            figurename = Fig.,
            tablename = Tab.]
            {caption}
\usepackage{tikz,pgfplots}  % to insert drawings and graphs
    \pgfplotsset{compat=newest}
    \usetikzlibrary{calc}   % to use some more advanced commands
    \usetikzlibrary{decorations.pathmorphing}
    %\usetikzlibrary{external}
    %\tikzexternalize[prefix = ./../Figures/tikz]
    \usetikzlibrary{patterns}

\usepackage{subfig}
\usepackage{interval}

% Citations and hyperlinks
\usepackage[backend = biber,
            sorting = none]{biblatex}
    \addbibresource{./../Bibliography/bibliography.bib}
\usepackage{csquotes}
\usepackage[hidelinks]{hyperref}
    \urlstyle{same}

 % To generate pfd in archive-friendly format
\usepackage[a-1b]{pdfx}